#include <stdio.h>
#include <stdlib.h>
/**
*add - adds two integers
*@a:integer for adding
*@b:integer for adding
*Return:the sum of two integers.
*/
int main(int a)
{
for (a > 0)
{
if ((a % 3 ==0) || (a % 5 == 0))
a++;
while (a < 1024)
{
printf(
return ((a + b));
}
