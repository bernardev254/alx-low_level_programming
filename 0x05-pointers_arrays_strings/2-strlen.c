#include "holberton.h"

/**
 *_strlen - returns the length of a string
 *@c: pointer
 *
 *Return:length of a string.
 */

int _strlen(char *s)
{
char[]s;
for (s = str; *s; s++)
{
return (s - str);
}
}
